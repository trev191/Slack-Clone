import React from 'react';
/**
 * Simple component with no state.
 * @return {object} JSX
 */
function UserProfile() {
  return (
    <div>
      UserProfile
    </div>
  );
}

export default UserProfile;
